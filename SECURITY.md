# 安全说明

AudioSpider 处理不可信 URL、网页响应和媒体文件。运行前请使用低权限账户，限制网络出口和磁盘配额，并确认你有权访问和使用目标内容。

## 报告安全问题

请通过仓库维护者提供的私密联系方式报告漏洞。不要在公开 issue 中粘贴：

- API Key、Secret、Cookie 或授权头；
- 带时效签名的完整媒体 URL；
- 私人音频、完整数据库或未脱敏日志；
- 能直接攻击仍在线系统的利用细节。

报告应包含受影响版本或 commit、最小复现、预期与实际行为、影响范围和脱敏日志。

## 支持边界

当前项目不会绕过登录、验证码、DRM、付费墙或平台访问控制，也不对第三方内容授予版权或训练许可。

YouTube 流水线只接受固定 HTTPS YouTube 单视频 URL，拒绝播放列表、直播和待开播内容；不读取 Cookie、账号或密码，限制 720p、单视频 8 GiB、父视频最长 4 小时和单次派生片段数。远程标题不会进入输出路径，yt-dlp 的临时媒体/字幕 URL 不写入 sidecar。

平台公开可访问不代表具备下载、切片、训练或再分发授权。`rights_cleared=false` 的视频与片段必须视作隔离候选，正式使用前需要独立权利审核。人工字幕与自动字幕分别标记为 `platform_manual`、`platform_auto`，不得把自动字幕伪装成人工标注。

辅助封面、字幕、章节和原文同样经过公网 DNS、重定向、MIME、数量与字节上限检查。HTML 会在保存前移除主动内容和危险 URL 协议；写入 sidecar 前会移除 URL 查询参数，持久化错误信息中的 URL 也会脱敏，避免保留 B站等来源的临时签名。

完整威胁模型、网络校验、资源限制和隐私说明见[安全边界文档](docs/design/security.md)。
